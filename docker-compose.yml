version: '3.8'
services:
  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DEPLOYMENT_ENV=local
    volumes:
      - ./src:/app/src:ro
    command: uvicorn src.app.main:app --host 0.0.0.0 --port 8000
